@model IEnumerable<Bd2Domain.Model.Organization>

@{
    ViewData["Title"] = "Index";
}

<h1>Організації</h1>

<p>
    <a asp-action="Create">Додати нову</a>
</p>
<form method="post">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Url)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Url)
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Id">Редагувати</a><br />
                        <button class="btn btn-danger mt-2" formaction="@Url.Action("Delete", new { id = item.Id })" formmethod="post">Видалити</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</form>
